namespace KeyVaultToAppConfig.IntegrationTests;

public sealed class AppConfigWriteTests
{
    [Fact(Skip = "Requires live Azure resources and credentials.")]
    public void ApplyRun_WritesExpectedChanges()
    {
        Assert.True(true);
    }
}
